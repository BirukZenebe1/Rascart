services:
  - type: web
    name: merkato-ai
    runtime: docker
    plan: starter
    autoDeploy: true
    envVars:
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET_KEY
        sync: false
